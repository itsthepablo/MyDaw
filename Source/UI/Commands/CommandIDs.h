#pragma once
#include <JuceHeader.h>

namespace DAWCommands {
    static const juce::CommandID playStop = 1;
    static const juce::CommandID toggleView = 2;
    static const juce::CommandID pause = 3;
    // Aquí irás añadiendo: record, undo, redo, delete, etc.
}